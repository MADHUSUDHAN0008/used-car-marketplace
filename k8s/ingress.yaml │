apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: used-car-ingress
spec:
  rules:
  - host: cars.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: used-car-service
            port:
              number: 80
